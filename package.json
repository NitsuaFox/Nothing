{
  "name": "nothing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 4177 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --host --port 4177 --strictPort"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
